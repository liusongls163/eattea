<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eattea.mapper.CategoryMapper">

    <select id="selectByType" resultType="com.eattea.entity.Category">
        SELECT * FROM eattea_category
        WHERE type = #{type}
        ORDER BY id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO eattea_category (name, type, parent_id)
        VALUES (#{name}, #{type}, #{parentId})
    </insert>

</mapper>
