<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eattea.mapper.ReportMapper">

    <select id="selectByProject" resultType="com.eattea.entity.Report">
        SELECT * FROM pm_report WHERE project_id = #{projectId} ORDER BY create_time DESC
    </select>

    <select id="selectByType" resultType="com.eattea.entity.Report">
        SELECT * FROM pm_report WHERE project_id = #{projectId} AND type = #{type}
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultType="com.eattea.entity.Report">
        SELECT * FROM pm_report WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pm_report (project_id, type, title, content, period_start, period_end)
        VALUES (#{projectId}, #{type}, #{title}, #{content}, #{periodStart}, #{periodEnd})
    </insert>

    <delete id="deleteById">
        DELETE FROM pm_report WHERE id = #{id}
    </delete>

    <delete id="deleteByProject">
        DELETE FROM pm_report WHERE project_id = #{projectId}
    </delete>

</mapper>
